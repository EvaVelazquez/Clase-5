const nombre = String("eva")
        const pwd = String("luz")
        if (nombre === "eva") {            
        } else if (pwd === "luz") {  
            console.log ("Tienes Acceso") 
            } else {
                console.log("No Tienes Acceso") 
                }